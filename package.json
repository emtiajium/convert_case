{
  "name": "convert_case",
  "version": "1.0.0",
  "description": "A minimal Electron application",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package-win": "electron-packager . \"Convert Case\" --overwrite --asar=true --platform=win32 --arch=ia32 --icon=assets/icon/icon.ico --prune=true --out=release-builds",
    "package-linux": "electron-packager . \"Convert Case\" --overwrite --asar=true --platform=linux --arch=x64 --icon=assets/icon/icon.png --prune=true --out=release-builds"
  },
  "repository": "https://bitbucket.org/emtiajium/convert_case",
  "keywords": [
    "Electron",
    "quick",
    "start",
    "tutorial",
    "demo"
  ],
  "author": "GitHub",
  "license": "CC0-1.0",
  "devDependencies": {
    "electron": "^3.0.0",
    "electron-packager": "^12.2.0"
  },
  "dependencies": {
    "autosize": "^4.0.2",
    "bootstrap": "^4.1.3",
    "jquery": "^3.3.1",
    "underscore.string": "^3.3.5"
  }
}
